{
  "name": "MyFirstSkill",
  "description": "A simple skill to log data",
  "version": "1.0.0",
  "functions": [
    {
      "name": "log_entry",
      "description": "Logs text to the screen",
      "parameters": {
        "type": "object",
        "properties": {
          "text": { "type": "string" }
        },
        "required": ["text"]
      }
    }
  ]
}
